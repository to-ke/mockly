# Live Transcription Configuration Example
# Copy relevant variables to your .env file in the project root

# ============================================================================
# REQUIRED: Deepgram API Credentials
# ============================================================================
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# ============================================================================
# LIVE TRANSCRIPTION SETTINGS
# ============================================================================

# Path to output JSON file with word-level timestamps
# - Can be relative (to backend directory) or absolute path
# - Leave empty or unset to disable live transcription
# - Default: live_transcription.json
LIVE_TRANSCRIPTION_PATH=live_transcription.json

# Update interval in seconds
# - How often to transcribe buffered audio and update JSON file
# - Lower values = more responsive, higher API usage
# - Higher values = less responsive, lower API usage
# - Recommended range: 1.5 - 5.0 seconds
# - Default: 2.0
LIVE_TRANSCRIPTION_UPDATE_INTERVAL=2.0

# ============================================================================
# AUDIO CONFIGURATION
# ============================================================================

# Deepgram TTS voice model
# Options: aura-2-thalia-en, aura-2-asteria-en, aura-2-arcas-en, etc.
# Default: aura-2-thalia-en
DEEPGRAM_TTS_VOICE=aura-2-thalia-en

# Audio encoding format
# Options: linear16 (PCM 16-bit), mulaw, alaw
# Default: linear16 (required for transcription)
DEEPGRAM_STREAM_ENCODING=linear16

# Audio sample rate in Hz
# Options: 8000, 16000, 24000, 48000
# Default: 48000 (higher quality, larger file size)
DEEPGRAM_SAMPLE_RATE=48000

# Deepgram STT (Speech-to-Text) model
# Options: nova-3, nova-2, whisper-large, etc.
# Default: nova-3 (best accuracy and speed)
DEEPGRAM_STT_MODEL=nova-3

# ============================================================================
# OTHER WORKFLOW SETTINGS
# ============================================================================

# Optional: Event-based TTS logging (different from live transcription)
# This creates NDJSON log of TTS events (appends, doesn't overwrite)
# Leave empty to disable
TTS_LIVE_JSON_PATH=

# Claude API settings
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# CORS settings for frontend
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# EXAMPLE CONFIGURATIONS
# ============================================================================

# High Responsiveness (every 1.5 seconds)
# LIVE_TRANSCRIPTION_UPDATE_INTERVAL=1.5

# Balanced (every 2.5 seconds) 
# LIVE_TRANSCRIPTION_UPDATE_INTERVAL=2.5

# Conservative (every 4 seconds, lower API usage)
# LIVE_TRANSCRIPTION_UPDATE_INTERVAL=4.0

# Custom output directory
# LIVE_TRANSCRIPTION_PATH=output/captions/live.json

# Disable live transcription
# LIVE_TRANSCRIPTION_PATH=

# Production setup with absolute path
# LIVE_TRANSCRIPTION_PATH=/var/www/mockly/static/live_transcription.json

