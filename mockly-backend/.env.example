# Backend service configuration
#
# Copy this file to `.env` (same folder) and fill in real values.
# Do not commit `.env` — it is already gitignored.

# Required API keys
ANTHROPIC_API_KEY=
DEEPGRAM_API_KEY=

# Optional model/version overrides
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Deepgram TTS/STT settings
DEEPGRAM_TTS_VOICE=aura-2-thalia-en
DEEPGRAM_STREAM_ENCODING=linear16
DEEPGRAM_SAMPLE_RATE=48000
DEEPGRAM_STT_MODEL=nova-3

# Optional: write live TTS events to a file (newline-delimited JSON)
#TTS_LIVE_JSON_PATH=./tts_live.jsonl

# Frontend CORS allow list (comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:5173

# Server host/port overrides (used by app.main __main__ guard)
#HOST=0.0.0.0
#PORT=8000
#UVICORN_RELOAD=0

